import { ComponentConfig } from '@morfeo/spec';

export const Table: ComponentConfig = {
  tag: 'table',
  states: {},
  style: {
    minWidth: '100',
    fontFamily: 'default',
    fontSize: 'm',
  },
  variants: {},
};
