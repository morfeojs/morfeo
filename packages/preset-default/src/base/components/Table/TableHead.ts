import { ComponentConfig } from '@morfeo/spec';

export const TableHead: ComponentConfig = {
  tag: 'thead',
  style: {
    borderBottom: 'strong',
    fontFamily: 'default',
    fontWeight: 'bold',
  },
  variants: {},
};
