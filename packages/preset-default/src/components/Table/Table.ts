import { ComponentConfig } from '@morfeo/web';

export const Table: ComponentConfig = {
  tag: 'table',
  states: {},
  style: {
    minWidth: '100',
    fontFamily: 'default',
    fontSize: 'm',
  },
  variants: {},
};
