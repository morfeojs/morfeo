---
sidebar_position: 1
id: getting-started
title: Getting started with Morfeo
description: Introduction > Getting started
---

Morfeo it's a tool to build design systems based on a theme.

It helps you to _follow a design language_ and write consistent UIs, whatever is the framework of your choice.

It's easy to use and, with the [**browser extension**](https://chrome.google.com/webstore/detail/morfeo/phhhjdmeicikchjnpepljcdgbmipipcl), your theme and your components are automagically documented.

## Features

- [Framework Agnostic](../Features/framework-agnostic)
- [Single source of truth](../Features/single-source-of-truth.mdx)
- [Multi theming](../Features/multi-theming)
- [Dev Tool](../Features/dev-tool.mdx)
- [CLI](../Features/CLI/cli.mdx)
- [Extendible](../Features/extendible.mdx)
- [Easy to test](../Features/easy-to-test.mdx)

## How it works

You can start using morfeo in 30 seconds:

- install

  ```bash
  # or @morfeo/(react | svelte | native | web) depending on your framework of choice
  npm i @morfeo/core
  # or with yarn
  yarn add @morfeo/core
  ```

- set the theme (you can create your own or use our [preset](../Packages/preset-default.mdx))

  ```typescript
  import { morfeo } from '@morfeo/core';
  import { darkTheme, lightTheme } from './themes';

  morfeo.setTheme('light', lightTheme);
  morfeo.setTheme('dark', darkTheme);
  ```

  or simply:

  ```typescript
  // automatically adds two default light and dark themes.
  import { initPreset } from '@morfeo/preset-default';

  initPreset();
  ```

- Use it everywhere to resolve a [morfeo style object](../theme-specification.mdx#morfeo-style-object) into a valid `css-in-js` object:

  ```tsx
  import { morfeo } from '@morfeo/core';

  /**
   * {
   *   "paddingLeft": "40px",
   *   "paddingRight": "40px"
   *   "borderRadius": "50%"
   *   "background-color": "#06f"
   * }
   */
  const style = morfeo.resolve({
    px: 'l',
    corner: 'round',
    bg: 'primary',
  });
  ```

Morfeo works everywhere, [no matter what's the framework or library you're using](../Features/framework-agnostic), not only as a `run time` parser but even as a static css generator thanks to our `CLI` ([@morfeo/cli](../Features/CLI/cli.mdx)).

Checkout all our packages and choose the one that fits better your needs:

- [@morfeo/react](../Packages/react)
- [@morfeo/native](../Packages/native)
- [@morfeo/styled-components-web](../Packages/styled-components)
- [@morfeo/svelte](../Packages/svelte)
- [@morfeo/jss](../Packages/jss)
- And more ...

And even more are coming soon:

- _@morfeo/angular_ **coming soon**
- _@morfeo/styled-components-native_ **coming soon**
