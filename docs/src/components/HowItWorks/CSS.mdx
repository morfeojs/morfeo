```css showLineNumbers {1,5}
.bg-primary {
  background-color: var(--colors-primary);
}

.py-m: {
  padding-top: var(--spacings-m);
  padding-bottom: var(--spacings-m);
}
```

```tsx /bg-primary py-m/
function Button() {
  return (
    <button className="bg-primary py-m">
      Click me
    </button>;
  )
}
```
