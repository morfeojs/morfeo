import { Steps } from 'nextra-theme-docs';

# Welcome 👋

In brief, Morfeo is a set of tools to build your **Design System** and **Style** your application, with nearly zero runtime thanks to its **Compiler**.

## Understand Morfeo

Too deeply understand what Morfeo is, and why you should use it in your workflow, you can think about Morfeo as a combination between 3 main parts:

<Steps style={{ position: 'relative' }}>

### 🎨 Theming system

That makes you build your own theme (**Design Language**).

### 💅 CSS-in-TS library

To _safely_ style your application by using your own theme.

### 🔄 Compiler

That transforms your styles into simple, optimized, plain CSS. This way you don't have to pay any price for the nice **Developer Experience** Morfeo is providing.

</Steps>

## Ready to start?

<Steps style={{ position: 'relative' }}>

### Write your component

```tsx {1, 3-7} /classes('button')/ copy showLineNumbers filename="Button.tsx"
import { morfeo } from '@morfeo/web';

const classes = morfeo.css({
  button: {
    bg: 'primary',
  },
});

function Button() {
  return <button className={classes('button')}>Hello, world</button>;
}
```

### Morfeo will optimize your code at **build time**

```tsx {1-5} /classes('button')/ showLineNumbers filename="Button.tsx"
const classes = function (...classNames) {
  // Interested on the internal implementation?
  // Check our GitHub!
  return 'bg-primary';
};

function Button() {
  return <button className={classes('button')}>Hello, world</button>;
}
```

> The process that happens at build time is properly described [here](./todo) in this article.

### And generate only the needed CSS

```css /.bg-primary/ showLineNumbers filename="morfeo.css"
.bg-primary {
  background-color: #0066ff;
}
```

</Steps>
