import { Steps } from 'nextra-theme-docs';

<Steps>

### Write your component

```tsx {1, 3-7} copy showLineNumbers filename="Button.tsx"
import { css } from '@morfeo/css';

const classes = css({
  button: {
    bg: 'primary',
  },
});

function Button() {
  return <button className={classes.button}>Hello, world</button>;
}
```

### Morfeo will optimize your code

```tsx {1-3} showLineNumbers filename="Button.tsx"
const classes = {
  button: 'bg-primary',
};

function Button() {
  return <button className={classes.button}>Hello, world</button>;
}
```

### And generate only the needed CSS

```css showLineNumbers filename="morfeo.css"
.bg-primary {
  background-color: #0066ff;
}
```

</Steps>
